from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Repo to analyze stroke gait data with CNN',
    author='Lin Zhou',
    license='MIT',
)
